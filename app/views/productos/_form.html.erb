<%= bootstrap_form_with(model: producto, local: true) do |form| %>
  <% if producto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(producto.errors.count, "error") %> prohibited this producto from being saved:</h2>

      <ul>
      <% producto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :articulo %>
  </div>

  <div class="field">
    <%= form.text_field :precio %>
  </div>

  <div class="field">
    <%= form.hidden_field :id_interna, :value => 0 %>
  </div>

  <div class="field">
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit :class => "btn btn-primary", :label => "Actualizar Producto" %>
  </div>
<% end %>