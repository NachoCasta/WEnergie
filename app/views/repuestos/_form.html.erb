<%= bootstrap_form_with(model: repuesto, local: true) do |form| %>
  <% if repuesto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(repuesto.errors.count, "error") %> prohibited this repuesto from being saved:</h2>

      <ul>
      <% repuesto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :articulo %>
    <%= form.text_field :articulo %>
  </div>

  <div class="field">
    <%= form.text_field :precio %>
  </div>

  <div class="field">
    <%= form.hidden_field :id_interna, :value => 0 %>
  </div>

  <div class="field">
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit :class => "btn btn-primary", :label => "Actualizar Repuesto" %>
  </div>
<% end %>
